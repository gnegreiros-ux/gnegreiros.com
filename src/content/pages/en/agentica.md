---
title: 'Agentica — A Design System Built on Decision Governance'
description: '800+ tokens, 70+ versioned ADRs, zero vendor lock-in. A personal design system proving that decisions can survive the tools that made them.'
heroTitle: 'Agentica — the decision system for humans and AI agents'
heroSubtitle: "A personal design system built to prove a thesis: design decisions should survive the tools, the frameworks, and the people who made them. Zero vendor lock-in."
---

Locked copy per `instructions/portfolio-gnegreiros-contenu-phase1.md` §3. Agentica's markup is bespoke (`src/pages/agentica.astro`) rather than rendered from this body — only the frontmatter (title/description/heroTitle/heroSubtitle) is consumed, for the `<head>` metadata and page hero. Hero deliberately omits the numeric metrics from the source copy (§4.6: no metric cards as the hero's opening move) — those numbers live in the results section instead, using the confirmed conservative figures (800+ tokens, 70+ ADRs, 14 components, 10 quality gates), not agentica.design's higher unconfirmed figures.
