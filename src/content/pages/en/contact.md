---
title: 'Contact — Guilherme Negreiros'
description: "Evaluating how to make design decisions durable across a team or organization? Let's talk about what that looks like in practice."
heroTitle: "Let's talk"
heroSubtitle: "If you're evaluating how to make design decisions durable — across a team, a product, or an organization — I'd be glad to talk through what that looks like in practice."
---

Locked copy per `instructions/portfolio-gnegreiros-contenu-phase1.md` §7. Contact's markup is bespoke (`src/pages/contact.astro`) rather than rendered from this body — only the frontmatter (title/description/heroTitle/heroSubtitle) is consumed, for the `<head>` metadata and page hero. Per §5, the page stays reduced to 2 levels (hero + contact details) — no closing note, even though the source doc marks one as optional. Resume/PDF intentionally omitted (deliverable not ready yet). LinkedIn/GitHub use the ArrowUpRight external-link icon rather than brand icons — `Linkedin`/`Github` aren't available in the installed `@lucide/astro` icon set (brand icons were removed upstream).
