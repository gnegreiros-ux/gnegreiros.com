// Canonical EN routes — the real content. fr/pt-br currently render the
// same fallback content (see instructions/CLAUDE EXECUTION PLAN — gnegreiros.md
// §14, translations out of scope for now), so testing them again adds
// little beyond what W3C validation already covers across every built file.
export const ROUTES = ['/', '/approche', '/agentica', '/dtgen', '/parcours', '/a-propos', '/contact'];
